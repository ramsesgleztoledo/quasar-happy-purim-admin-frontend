export interface MemberCategoryInterface {
  categoryId: number;
  categoryName: string;
  selected: boolean;
}

export interface ShulCategoryInterface {
  categoryID: number;
  categoryName: string;
}
