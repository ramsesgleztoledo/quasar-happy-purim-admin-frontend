import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.happypurimadmin',
  appName: 'HappyPurimAdmin',
  webDir: 'dist/spa',

};

export default config;
